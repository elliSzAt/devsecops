# Checkov config — tune for your org (example skips for demo VPC)
# Remove skips in production and enable Flow Logs, etc.

framework:
  - terraform

compact: true

# VPC Flow Logs (CKV2_AWS_11) — add aws_flow_log + CloudWatch in production
skip-check:
  - CKV2_AWS_11
